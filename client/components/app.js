import React from 'react';
import Header from './header';
import BinList from './bins/bins_list';

export default () => {
    return(
        <div>
            <Header />
            <BinList />
        </div>
    );
}